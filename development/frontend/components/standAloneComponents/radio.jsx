// Builds radiobutton.

import React, { useState } from 'react';



const Radio = ({ showIndicator, labels }) => {
    const [showIndicator, setShowIndicator] = useState(data); // setTableData is the updater function
    
    const permissions = [
        'view chickens',
        'edit chickens',
        'add external data',
        'export to excel',
        'create/delete pairings',
        'create user',
        'delete user'
    ]
    
    return(
        <>
            <section className='permissions-panel'>
               

            </section>        
        </>
    )
}
